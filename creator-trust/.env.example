# Leave empty to call the API at the same origin (e.g. when Vite's dev
# proxy handles it, or when frontend + backend are deployed together).
# Set to your deployed backend URL otherwise, e.g.:
# VITE_API_BASE=https://asliconnect-backend.onrender.com
VITE_API_BASE=
